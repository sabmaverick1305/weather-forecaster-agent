server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # /api/* is handled by the ALB before reaching nginx; no proxy needed here.

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
